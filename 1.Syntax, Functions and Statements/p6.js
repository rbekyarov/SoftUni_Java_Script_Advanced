function solve(N, M){
    let startNum = Number (N);
    let endNum = Number (M);
    let sum = 0;

    for (let index = startNum; index <= endNum; index++) {
        
        sum+=index;
        
        
    }
    console.log(sum);
}
//solve('1', '5');
solve('-8', '20');